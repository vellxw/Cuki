# Log histórico

`verify-final.log` es un archivo recuperado de la conversación previa y preservado sin modificaciones.

No se ejecutaron sus comandos en esta publicación. Faltan los tests y el proyecto que produjo el registro. Las cifras incluidas en él no certifican que la rama actual compile o pase esas pruebas.

La comprobación actual es únicamente de identidad de bytes mediante `MANIFEST.sha256` y los Git blob SHA del inventario. No se incluye APK, IPA, captura de emulador ni evidencia de pagos reales.
