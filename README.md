# CUKI · Native implementation in progress

**Incomplete, not yet an installable delivery.** The original recovered screens are being integrated with a reconstructed React Native / Expo application, SQLite persistence, shared deterministic domain and remote adapters. See `PROJECT_STATE.md` for the actual verification status.

The canonical Home art direction is Botanical Liquid Glass and is not being redesigned. The node_modules directory, private blueprint inspiration references, service secrets and personal databases are excluded from the source.

Node 22.16.0; npm workspaces; `npm ci` is the intended reproducible installation. TypeScript can be checked with `npm run typecheck`. Backend scripts, automated verification and native builds are still being completed in this checkpoint. Do not interpret the historical log as current verification.
