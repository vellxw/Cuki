import * as Notifications from 'expo-notifications';import {Platform} from 'react-native';
export async function cancelRest(id:string|null|undefined){if(id)await Notifications.cancelScheduledNotificationAsync(id)}
export async function requestNotifications(){if(Platform.OS==='android')await Notifications.setNotificationChannelAsync('rest',{name:'Descansos',importance:Notifications.AndroidImportance.DEFAULT});const p=await Notifications.getPermissionsAsync();return p.granted||(await Notifications.requestPermissionsAsync()).granted}
export async function scheduleRest(deadline:number,previous?:string|null){await cancelRest(previous);if(deadline<=Date.now())return null;const permission=await Notifications.getPermissionsAsync();if(!permission.granted)return null;return Notifications.scheduleNotificationAsync({content:{title:'Descanso terminado',body:'Tu sesión sigue guardada. Continuá cuando estés preparado.',sound:'default',data:{route:'training'}},trigger:{type:Notifications.SchedulableTriggerInputTypes.DATE,date:new Date(deadline),...(Platform.OS==='android'?{channelId:'rest'}:{})}})}
