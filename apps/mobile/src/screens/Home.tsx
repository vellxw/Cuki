import React,{useState} from 'react';
import {View,Pressable,useWindowDimensions,StyleSheet} from 'react-native';
import {Image} from 'expo-image';
import {LinearGradient} from 'expo-linear-gradient';
import {useApp,useTask} from '../data/AppProvider';
import {Screen,Txt,Title,Button,IconButton,Message,Row,Ring,MacroRow,Section,Empty,useNav,DateControl,confirm,type ScreenProps,art} from '../ui/components';
import {Plant} from '../ui/Plant';
import {Icon} from '../ui/Icon';
import {layout,useTheme} from '../ui/theme';
import {dayNutrition,goalAt,fmt,prettyDate,localDate} from '../../../../packages/core/utils';
import {MEALS,type Meal} from '../../../../packages/core/types';
export function Home({params={}}:Partial<ScreenProps>){const{state}=useApp();const nav=useNav();const{width,fontScale}=useWindowDimensions();const{c}=useTheme();const calories=dayNutrition(state,state.selectedDate);const goal=goalAt(state,state.selectedDate);const g=state.garden;const week=g?.creditedWeeks??0;const active=state.sessions.find(s=>s.status==='active'||s.status==='paused');const plan=state.plans[0];const collapsed=width<370||fontScale>1.3;
 return <Screen back={false} dock={false} background testID="SC-07"><View style={[layout.between,{paddingTop:18,alignItems:'flex-start'}]}><View style={{flex:1}}><Txt size={30}>Buenos días{state.profile.name?',':''}</Txt>{state.profile.name&&<Txt size={38} weight="700">{state.profile.name}</Txt>}<Txt tone="secondary" size={14} style={{marginTop:10}}>Disciplina hoy,{"\n"}un mejor mañana.</Txt></View><Pressable accessibilityRole="button" accessibilityLabel="Abrir perfil y ajustes" onPress={()=>nav.go('SC-68')} style={{width:44,height:44,borderRadius:22,borderWidth:1,borderColor:c.line,backgroundColor:c.surface,alignItems:'center',justifyContent:'center'}}><Txt weight="600">{state.profile.name?state.profile.name.slice(0,2).toUpperCase():'Vos'}</Txt></Pressable></View><View style={{flexDirection:collapsed?'column':'row',gap:10,alignItems:collapsed?'stretch':'center',minHeight:270}}><View style={{flex:1,gap:15,alignItems:collapsed?'center':'flex-start'}}><Pressable accessibilityRole="button" accessibilityLabel="Abrir diario de nutrición" onPress={()=>nav.go('SC-08')} style={[layout.row,{minHeight:48}]}><Icon name="recipes" color={c.text} size={18}/><Txt size={13} weight="500">Nutrición de hoy</Txt><Icon name="arrow" size={14}/></Pressable>{state.profile.showCalories?<Ring value={calories.energy_kcal} goal={goal?.nutrients.energy_kcal} size={Math.min(158,(width-50)*.46)}/>:<Txt size={20} weight="600">Tu diario, sin calorías</Txt>}<MacroRow nutrients={calories} showEnergy={false}/>{!goal&&<Pressable onPress={()=>nav.go('SC-04')} accessibilityRole="button" style={{minHeight:44,justifyContent:'center'}}><Txt tone="secondary" size={12}>Configurar metas opcionales →</Txt></Pressable>}</View>{!state.profile.hideGarden&&<View style={{flex:1.06,alignItems:'center'}}><Plant seed={g?.plant.seed??state.localPlantSeed} weeks={week} height={235}/><Pressable accessibilityRole="button" accessibilityLabel="Ver tu jardín" onPress={()=>nav.go('SC-79')} style={{alignSelf:'stretch',gap:8,marginTop:-8,minHeight:72}}><View style={layout.row}><Icon name="leaf" color={c.protein} size={17}/><Txt size={15} weight="600">Tu planta</Txt></View><Txt size={13} tone="secondary">{g?`${week} de 52 semanas acreditadas`:'Tu historia empieza con una semilla'}</Txt><View style={{height:5,borderRadius:4,backgroundColor:c.line,overflow:'hidden'}}><View style={{height:5,width:`${week/52*100}%`,backgroundColor:c.protein}}/></View><Txt size={11} tone="secondary">{g?.state==='interrupted'?'Tu planta se conserva. Podés empezar otro ciclo.':g?'Una sesión por semana, a tu ritmo.':'Plantá tu primer ciclo cuando estés listo.'}</Txt></Pressable></View>}</View><Button title="Registrar" icon="plus" onPress={()=>nav.register({date:state.selectedDate})} testID="home-register" style={{marginVertical:6}}/><View style={{borderTopWidth:StyleSheet.hairlineWidth,borderTopColor:c.line,paddingTop:22,gap:13}}><View style={layout.row}><Icon name="train" size={19}/><Txt size={14} tone="secondary">{active?'Sesión en curso':'Siguiente entrenamiento'}</Txt></View><Txt size={28} weight="600">{active?active.name:plan?plan.days[0]?.name??plan.name:'Construí tu rutina'}</Txt><Txt size={14} tone="secondary">{active?`${active.exercises.length} ejercicios · tus series están guardadas`:plan?`${plan.days[0]?.exercises.length??0} ejercicios · plan manual`:'Elegí ejercicios, series y días a tu medida.'}</Txt><Button title={active?'Reanudar sesión':plan?'Ver rutina':'Crear rutina'} icon="arrow" variant="secondary" onPress={()=>active?nav.go(active.restDeadline?'SC-48':'SC-47',{id:active.id}):plan?nav.go('SC-42',{planId:plan.id}):nav.go('SC-44')} style={{alignSelf:'flex-start'}}/></View><View style={layout.wrap}><Button title="Diario" variant="quiet" onPress={()=>nav.go('SC-08')}/><Button title="Mi semana" variant="quiet" onPress={()=>nav.go('SC-36')}/></View><View style={{height:100}}/></Screen>;
}
export function Diary(){const{state,repo}=useApp();const nav=useNav();const task=useTask();const [deleted,setDeleted]=useState<string|null>(null);const[dateText,setDateText]=useState(state.selectedDate);const entries=state.diary.filter(e=>e.date===state.selectedDate&&!e.deletedAt);const totals=dayNutrition(state,state.selectedDate);
 return <Screen title="Tu diario" subtitle="Cada registro conserva los nutrientes de su versión original." testID="SC-08"><DateControl value={dateText} onChange={value=>{setDateText(value);if(/^\d{4}-\d{2}-\d{2}$/.test(value))void task.run(()=>repo.dispatch({type:'date',date:value}));}}/><MacroRow nutrients={totals}/><Button title="Añadir alimento" icon="plus" onPress={()=>nav.register({date:state.selectedDate})}/>{(Object.keys(MEALS) as Meal[]).map(meal=><Section key={meal} title={MEALS[meal]} action="Añadir" onAction={()=>nav.register({date:state.selectedDate,meal})}>{entries.filter(e=>e.meal===meal).length?entries.filter(e=>e.meal===meal).map(e=><Row key={e.id} title={e.name} subtitle={`${fmt(e.quantity,1)} ${e.basis==='servings'?'porciones':e.basis==='milliliters'?'ml':'g'} · ${fmt(e.nutrients.energy_kcal)} kcal`} onPress={()=>nav.go('SC-12',{entryId:e.id})} trailing={<IconButton name="trash" label={`Eliminar ${e.name}`} onPress={()=>confirm('Eliminar registro','Podés deshacerlo sin volver a calcular nutrientes.',()=>task.run(async()=>{await repo.dispatch({type:'deleteEntry',id:e.id});setDeleted(e.id);}),true)}/>}/>):<Txt tone="muted" size={14}>Sin registros en esta comida.</Txt>}</Section>)}{deleted&&<Button title="Deshacer eliminación" variant="secondary" onPress={()=>task.run(async()=>{await repo.dispatch({type:'restoreEntry',id:deleted});setDeleted(null);})}/>}<Button title="Ver tendencias" variant="quiet" onPress={()=>nav.go('SC-58')}/><Message type="error">{task.error}</Message></Screen>;
}
export const homeScreens={'SC-07':Home,'SC-08':Diary};
